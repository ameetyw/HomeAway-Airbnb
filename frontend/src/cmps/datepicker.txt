basic day
    react-datepicker__day
    react-datepicker__day--014 (date #)
    (react-datepicker__day--today)
    (react-datepicker__day--weekend)

added to first day of selected:
    react-datepicker__day--selected (if in selecting start mode)
    react-datepicker__day--range-start 
    react-datepicker__day--in-range 
    react-datepicker__day--in-selecting-range (removed when hover to shorten start)
    react-datepicker__day--selecting-range-start (removed when hover to change-+ start)

on hover to change start: (on hovered days)
    react-datepicker__day--in-range (to shorten start)
    react-datepicker__day--in-selecting-range
    react-datepicker__day--selecting-range-start (first)

day within selected range:
    react-datepicker__day--in-range
    *react-datepicker__day--in-selecting-range 

end selected day
    react-datepicker__day--selected (if in selecting end mode)
    react-datepicker__day--range-end
    react-datepicker__day--in-range
    react-datepicker__day--in-selecting-range (removed when hover to shorten end)
    react-datepicker__day--selecting-range-end (removed when hover to change-+ end)

on hover to extend end: (days in new range)
    react-datepicker__day--in-selecting-range
    react-datepicker__day--selecting-range-end (last)

on hover to shorten end (days outside new range):
    react-datepicker__day--in-range
    react-datepicker__day--in-selecting-range (last)

