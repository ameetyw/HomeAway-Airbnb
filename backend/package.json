{
  "name": "server",
  "version": "1.0.0",
  "description": "HomeAway server",
  "main": "server.js",
  "scripts": {
    "start": "set (NODE_ENV=development&&nodemon server.js || NODE_ENV=production&&node server.js)",
    "server:dev": "set NODE_ENV=development&&nodemon server.js",
    "server:prod": "set NODE_ENV=production&&node server.js"
  },
  "author": "Amitee",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-session": "^1.17.2",
    "mongodb": "^4.2.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
